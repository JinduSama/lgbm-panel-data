keep going to generate really good insight and understanding to the topic with a good variey of scenarios (stationarity, trends, how to prepare data, features not only target lags also other features that can describe the data) so that I can get a good overview. Present it a structered html file. Also make reasonable commits and push to github.

What you often also want is, especially when using lgbm for such a task is causal plausability from input variable to target variables. Since using the target vatiable (with lags) in the model itsself leads to good prediction due to high correlation from target lags to future targets. However the eplainability isnt given then which comes from other explanotary variables. Therefore lgbm can be better then other classical timeseries models. We should explore that path as well.